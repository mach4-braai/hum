# hum

Local-first daemon and CLI that renders work-session lifecycle events as an ambient musical soundscape: one work session maps to one sustained drone in a chosen key and scale, with completion and failure phrases, driven by a provider-agnostic event protocol over a Unix socket.
