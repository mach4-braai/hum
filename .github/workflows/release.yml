name: Release

on:
  push:
    branches: [master]
    tags: ["v*"]

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  snapshot:
    name: snapshot
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654
        with:
          cache: false

      - name: "Install the audio and cross-compilation packages (drop with oto v3.5, #39)"
        uses: ./.github/actions/linux-packages

      - name: Snapshot
        env:
          HUM_RELEASE_LINUX: "1"
        run: mise run snapshot

  check:
    name: check
    if: startsWith(github.ref, 'refs/tags/v')
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654
        with:
          cache: false

      - name: "Install Linux audio build dependency (drop with oto v3.5, #39)"
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev pkg-config

      - run: mise run check

  windows:
    name: windows
    if: startsWith(github.ref, 'refs/tags/v')
    strategy:
      fail-fast: true
      matrix:
        os: [windows-latest, windows-11-arm]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654
        with:
          cache: false

      - run: go vet ./...

      - name: Vet the other Windows architecture
        if: matrix.os == 'windows-latest'
        run: go vet ./...
        env:
          GOARCH: arm64

      - name: Serve and stop a real daemon
        shell: bash
        run: |
          set -eu
          mise run build
          export HUM_HOME="$RUNNER_TEMP/hh"
          export HUM_SOCKET="$HUM_HOME/humd.sock"
          mkdir -p "$HUM_HOME"
          ./bin/humd.exe --no-audio --log-level debug &
          for _ in $(seq 100); do [ -e "$HUM_SOCKET" ] && break; sleep 0.2; done
          ./bin/hum.exe status
          ./bin/hum.exe daemon stop
          test ! -e "$HUM_SOCKET" || { echo "the socket outlived the daemon" >&2; exit 1; }

  release:
    name: release
    needs: [check, windows]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write # goreleaser creates the draft release and uploads its assets
      id-token: write # actions/attest-build-provenance signs the attestation via OIDC
      attestations: write # actions/attest-build-provenance uploads the attestation bundle
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654
        with:
          cache: false

      - name: "Install the audio and cross-compilation packages (drop with oto v3.5, #39)"
        uses: ./.github/actions/linux-packages

      - name: Release
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GORELEASER_CURRENT_TAG: ${{ github.ref_name }}
          HUM_RELEASE_LINUX: "1"
        run: goreleaser release --clean

      - name: Attest release artifacts
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8
        with:
          subject-path: |
            dist/*.tar.gz
            dist/*.zip
            dist/checksums.txt
